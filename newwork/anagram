def make_exercise(str1, str2):
  if len(str1) != len(str2):
    return False
    
  if sorted(str1) != sorted(str2):
    return False
  return True 
    
verify = make_exercise("anagram", "nagaarm")
print(verify)  
  
